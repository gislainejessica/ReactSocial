import React from 'react'
import { View, Text } from 'react-native'

const types = () => {
    return (
        <View>
            <Text> Tipos de algo </Text>
        </View>
    )
}

export default types
